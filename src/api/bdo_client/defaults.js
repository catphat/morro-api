const transportOptions = {
  timeout: 10000,
};

const defaultHeaders = {
  'Content-Type': 'application/json',
  'User-Agent': 'BlackDesert',
};

module.exports = {
  defaultHeaders,
  transportOptions,
};
